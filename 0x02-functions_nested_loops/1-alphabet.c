#include "main.h"

/**
 * main - print the alphabets in lowercase
 *
 * Return: Always 0.
 */
int main(void)
{
	char print_alphabet = ch;
	for (ch = 'a'; ch <= 'z'; ch++);
	_putchar(ch);
	return (0);
}
